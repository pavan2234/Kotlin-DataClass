fun main() {
    val ch = charArrayOf('a','b','c','s','d')
    val d = String(ch)
    println(d)
    //above code is the traditional one but directly we can declare

    var a ="This is the double qoute"
    var b ="""This is the triple quote"""
    println(a)
    println(b)
}